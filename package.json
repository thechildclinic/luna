{
  "name": "luna---ai-journaling-companion",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "node deploy.js",
    "deploy:validate": "node scripts/validate-deployment.js",
    "deploy:setup": "node scripts/env-setup.js",
    "deploy:check": "node scripts/validate-deployment.js --pre-deploy",
    "deploy:build": "node scripts/validate-deployment.js --build",
    "deploy:verify": "node scripts/validate-deployment.js --post-deploy",
    "generate:icons": "node generate-icons.js"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "@google/genai": "^1.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  }
}
